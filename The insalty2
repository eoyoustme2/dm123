-- Chờ LocalPlayer được khởi tạo
while not game.Players.LocalPlayer do wait() end
local player = game.Players.LocalPlayer

        -- Hiện caption
        require(player.PlayerGui.MainUI.Initiator.Main_Game).caption("Đang tải tài nguyên...", true)
        wait(2)
        require(player.PlayerGui.MainUI.Initiator.Main_Game).caption("", true)

          if game:GetService("ReplicatedStorage").GameData.Floor.Value == "Hotel" then
           if game.ReplicatedStorage.GameData.LatestRoom.Value == 7 then


        -- Tải Crucifix & Sprint Bar
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/eoyoustme2/dm123/refs/heads/main/Intro%20insalty"))()
        end)

        -- Danh sách entity cần tải
        local entityList = {
            {url = "https://raw.githubusercontent.com/eoyoustme2/dm123/refs/heads/main/Fallacy", delay = 170},
            {url = "https://raw.githubusercontent.com/eoyoustme2/dm123/refs/heads/main/Hallowen12", delay = 240},
            {url = "https://raw.githubusercontent.com/eoyoustme2/-i-lost-my-account-is-eoyoustme-/refs/heads/main/CROUCH", delay = 195},
        }

        -- Tạo các coroutine tải entity
        for _, entity in ipairs(entityList) do
            coroutine.wrap(function()
                while true do
                    game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
                    wait(entity.delay)
                    pcall(function()
                        loadstring(game:HttpGet(entity.url))()
                    end)
                end
            end)()
        end
    end
end
